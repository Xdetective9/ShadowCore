services:
  - type: web
    name: shadowcore
    env: node
    region: singapore
    plan: free
    buildCommand: |
      npm install
      npm run setup
    startCommand: npm start
    healthCheckPath: /health
    # Add these lines:
    envVars:
      - key: PORT
        value: 10000  # Change to 10000 for Render
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: OWNER_NAME
        value: Abdullah
      - key: OWNER_CONTACT
        value: "+923288055104"
      - key: ADMIN_PASSWORD
        value: Rana0986
      - key: REMOVEBG_API_KEY
        value: xv5aoeuirxTNZBYS5KykZZEK

databases:
  - name: shadowcore-db
    plan: free
    databaseName: shadowcore
    user: shadowcore_user
